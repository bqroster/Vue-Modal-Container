export default {
    props: {
        isBgVisible: {
            type: Boolean,
            default: true
        },

        isScrollBlocked: {
            type: Boolean,
            default: true
        },
    },
}